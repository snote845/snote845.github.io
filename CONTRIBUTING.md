# 贡献指南

感谢您对本博客项目的关注！本文档将指导您如何为项目做出贡献。

## 📝 目录

- [行为准则](#行为准则)
- [如何贡献](#如何贡献)
- [开发流程](#开发流程)
- [代码规范](#代码规范)
- [提交规范](#提交规范)

## 🤝 行为准则

- 尊重所有贡献者
- 建设性地提出反馈
- 专注于对项目最有益的改进

## 💡 如何贡献

### 报告问题

如果您发现了 bug 或有功能建议：

1. 检查 [Issues](https://github.com/snote845/snote845.github.io/issues) 确保问题尚未被报告
2. 使用清晰的标题描述问题
3. 提供详细的重现步骤（如果是 bug）
4. 附上相关的截图或错误日志

### 提交代码

1. **Fork 本仓库**
2. **创建特性分支** (`git checkout -b feature/AmazingFeature`)
3. **提交更改** (`git commit -m 'Add some AmazingFeature'`)
4. **推送到分支** (`git push origin feature/AmazingFeature`)
5. **开启 Pull Request**

## 🛠 开发流程

### 环境准备

```bash
# 克隆仓库
git clone https://github.com/snote845/snote845.github.io.git
cd snote845.github.io

# 安装依赖（需要 Ruby 3.3+）
bundle install

# 启动本地服务器
bundle exec jekyll serve

# 访问 http://localhost:4000
```

### 项目结构

```
├── _posts/          # 博客文章（按分类组织）
├── _tabs/           # 页面标签
├── _includes/       # 自定义组件
├── assets/          # 静态资源
├── _config.yml      # 站点配置
├── tools/           # 部署工具
└── .github/         # GitHub 配置
```

### 编写文章

1. 在 `_posts/` 对应分类目录下创建 Markdown 文件
2. 文件命名格式：`YYYY-MM-DD-title.md`
3. 添加 Front Matter：

```yaml
---
layout: post
title: "文章标题"
date: YYYY-MM-DD HH:MM:SS +0800
categories: [分类]
tags: [标签1, 标签2]
description: "文章摘要（可选）"
---
```

## 📐 代码规范

### Markdown 规范

- 使用中文标点符号
- 代码块指定语言：\```java ... \```
- 图片使用相对路径或 CDN
- 文章标题使用明确的描述性名称

### Jekyll 配置

- 修改 `_config.yml` 前请先了解配置项含义
- 测试构建是否成功：`bundle exec jekyll build`
- 检查生成的 HTML 是否正确

### 图片优化

```bash
# 推荐使用 WebP 格式
cwebp -q 80 input.png -o output.webp

# 或使用 picture 标签
{% include img.html src="image.webp" alt="描述" width="800" height="450" %}
```

## 📬 提交规范

### Commit Message 格式

```
<type>(<scope>): <subject>

<body>

<footer>
```

### Type 类型

- `feat`: 新功能
- `fix`: 修复 bug
- `docs`: 文档更新
- `style`: 代码格式（不影响功能）
- `refactor`: 重构
- `perf`: 性能优化
- `test`: 测试
- `chore`: 构建过程或工具变更

### 示例

```bash
git commit -m "feat(posts): 添加搜索功能"
git commit -m "fix(seo): 修复语言设置为中文"
git commit -m "docs(readme): 更新部署说明"
```

## ✅ Pull Request 检查清单

提交 PR 前请确保：

- [ ] 代码通过本地构建测试
- [ ] 遵循代码规范
- [ ] 添加了必要的文档
- [ ] Commit message 符合规范
- [ ] 没有引入新的警告

## 📧 联系方式

- Email: android_gr@163.com
- GitHub: [@snote845](https://github.com/snote845)

## 📄 许可证

本项目采用 MIT 许可证 - 查看 [LICENSE](LICENSE) 文件了解详情。
